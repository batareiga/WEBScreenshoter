const form = document.getElementById('form'); const urlInput = document.getElementById('url'); const screenshotImg = document.getElementById('screenshot'); form.addEventListener('submit', async (e) => { e.preventDefault(); const url = urlInput.value; try { const browser = await puppeteer.launch({ headless: true }); const page = await browser.newPage(); await page.goto(url); await page.screenshot({ path: 'screenshot.png', fullPage: true }); await browser.close(); screenshotImg.src = 'screenshot.png'; } catch (err) { console.error(err); } });
